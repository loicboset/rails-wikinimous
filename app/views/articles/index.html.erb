
<% @articles.each do |article| %>
  <div class="articles_card container">
    <%= link_to article_path(article) do %>
      <h2> <%= article.title %> </h2>
      <p> <%= article.content %> </p>
    <% end %>
  </div>
<% end %>
